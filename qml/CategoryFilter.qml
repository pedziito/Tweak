import QtQuick 2.15
import QtQuick.Layouts 1.15

/// Category filter — now a stub since filtering is built into TweaksPage tabs.
/// Kept for CMake compatibility.
Flow {
    id: filterRoot
    spacing: 0
    visible: false
}
